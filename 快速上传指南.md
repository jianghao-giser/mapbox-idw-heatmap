# 🚀 快速上传到 GitHub

## 方式一：自动化脚本（推荐）

```bash
cd /Users/xwqin/Desktop/mapbox-idw-heatmap
./upload-to-github.sh
```

按提示输入你的 GitHub 用户名即可！

---

## 方式二：手动上传

### 1️⃣ 在 GitHub 创建仓库

访问：https://github.com/new

- 仓库名: `mapbox-idw-heatmap`
- ⚠️ **不要**勾选任何初始化选项

### 2️⃣ 执行命令

```bash
cd /Users/xwqin/Desktop/mapbox-idw-heatmap

# 替换 `你的用户名` 为你的 GitHub 用户名
git remote add origin https://github.com/你的用户名/mapbox-idw-heatmap.git

git branch -M main
git push -u origin main
```

### 3️⃣ 完成！

访问 `https://github.com/你的用户名/mapbox-idw-heatmap` 查看仓库

---

## 📦 项目包含的文件

✅ **核心代码**
- `src/MapboxIDWHeatmapLayer.ts` - IDW 热力图图层
- `src/index.ts` - 导出入口

✅ **配置文件**
- `package.json` - npm 包配置
- `tsconfig.json` - TypeScript 配置  
- `rollup.config.js` - 构建配置

✅ **文档**
- `README.md` - 完整使用文档（中文）
- `LICENSE` - MIT 开源许可证
- `GITHUB_UPLOAD_GUIDE.md` - 详细上传指南

✅ **示例**
- `examples/RainHeatmap.example.vue` - Vue 3 完整示例
- `examples/README.md` - 示例说明

---

## 🎯 下一步

### 选项1: 发布到 npm（让别人可以 npm install）

```bash
npm run build
npm login
npm publish
```

### 选项2: 继续开发

```bash
# 安装依赖
npm install

# 构建
npm run build

# 开发模式（监听文件变化）
npm run dev
```

### 选项3: 添加 GitHub Actions

创建 `.github/workflows/publish.yml` 实现自动发布到 npm

---

## ⭐ 项目特点

- 🚀 **高性能** - WebGL GPU 加速
- 🎨 **灵活** - 自定义 GLSL 着色器配色
- ✂️ **精确** - AOI 多边形裁剪
- 📦 **现代** - TypeScript + ES Module
- 📝 **完整** - 详细文档和示例

---

## 📞 需要帮助？

查看 `GITHUB_UPLOAD_GUIDE.md` 获取详细帮助文档
